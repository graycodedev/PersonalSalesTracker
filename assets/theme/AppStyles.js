import { StyleSheet } from 'react-native';

const AppStyles = StyleSheet.create({
    DetailText:{
        KeyText:{
            fontSize: 16,
        color: "#333",
        }
    }

});
export default AppStyles